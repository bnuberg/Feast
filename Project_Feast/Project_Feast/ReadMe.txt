Config file is set up so that you can run:
doxygen.exe doxygen.cfg
from the command line and it will generate documentation for the files in the current directory in the html and latex folders.
The name of the project will probably need changing though.